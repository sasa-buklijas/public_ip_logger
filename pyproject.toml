[project]
name = "public-ip-logger"
version = "1.1.7"
description = "Log public IP address in SQLite database"
readme = "README.md"
requires-python = ">=3.11.2"
dependencies = [
    "dataset>=1.6.2",
    "humanize>=4.12.3",
    "ping3>=5.1.5",
    "requests>=2.32.5",
]
